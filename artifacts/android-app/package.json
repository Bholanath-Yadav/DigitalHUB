{
  "name": "@workspace/android-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "pnpm --dir ../gaming-store build",
    "cap:sync": "pnpm run build:web && cap sync android",
    "cap:add": "cap add android",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/cli": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  }
}