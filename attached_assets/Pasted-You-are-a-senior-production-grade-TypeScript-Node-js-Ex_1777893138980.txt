You are a senior production-grade TypeScript + Node.js + Express + Drizzle ORM engineer.

I have a pnpm monorepo backend project that fails both locally and during Vercel deployment due to TypeScript errors.

Your job is to FULLY FIX the project so it builds successfully locally AND on Vercel without changing business logic.

---

## ❗ Current Issues (MUST FIX ALL)

### 1. TS7030: Not all code paths return a value
- Happens in Express route handlers
- Files affected: chat.ts, users.ts, orders.ts, payments.ts, coupons.ts, products.ts, storage.ts, admin.ts
- Cause: missing `return` in conditional responses

### 2. TS2769: No overload matches this call (Drizzle ORM)
- Occurs in database queries using `eq()`, `where()`, `insert()`
- Cause: passing `string | string[]` instead of `string`
- Affects fields like:
  - supabase_id
  - session_id
  - method
  - id filters

### 3. Express request typing issues
- req.query and req.body are untyped
- Values come as string | string[]
- Must be safely normalized before DB usage

### 4. Drizzle ORM strict typing errors
- PgColumn type mismatch
- "never" overload errors
- incorrect column comparisons

### 5. Build failure in CI/Vercel
- pnpm run typecheck (tsc --noEmit) fails
- Must pass strict TypeScript compilation

---

## 🎯 REQUIRED FIX STRATEGY

### STEP 1: Global Input Normalization (MANDATORY)
Create reusable helper:

- Convert ANY input (req.query, req.body, params) safely to string
- Handle string | string[] properly

Example rule:
- If array → take first value
- Always return string

---

### STEP 2: Fix ALL Express Route Handlers
- Every route MUST:
  - return response in ALL branches
  - use `return res.status().json()` pattern
  - never leave execution hanging

---

### STEP 3: Fix Drizzle ORM Queries
- NEVER pass string | string[]
- Always normalize BEFORE using eq(), where(), insert()

Correct pattern:
- const id = toString(req.query.id)
- eq(table.column, id)

---

### STEP 4: Fix Insert/Update Payloads
- Ensure all DB insert objects match schema strictly
- Remove undefined or wrong-typed fields
- Fix method, label, id mismatches

---

### STEP 5: Ensure TypeScript Clean Build
- No implicit any errors
- No unreachable code warnings
- No overload errors
- No missing return paths

---

## 🧠 STRICT RULES

- DO NOT remove features or endpoints
- DO NOT change business logic
- ONLY fix types, returns, and data handling
- KEEP code production-ready
- ENSURE pnpm monorepo build passes
- ENSURE Vercel deployment succeeds

---

## ✅ FINAL OUTPUT REQUIREMENT

After fixes:
- pnpm run typecheck must pass
- pnpm run build must pass
- Vercel deployment must succeed
- No TS errors remain anywhere

Start by identifying repeated patterns, then apply global fixes, then fix each file systematically.